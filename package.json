{
  "name": "factory-web",
  "version": "1.0.0",
  "private": true,
  "description": "工厂原型 - Factorio clone in browser",
  "scripts": {
    "build": "node build.js",
    "build:minify": "node build.js --minify"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
